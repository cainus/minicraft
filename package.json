{
  "name": "minicraft",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port ${PORT:-5173}",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "~6.0.2",
    "vite": "^8.1.0"
  },
  "dependencies": {
    "@types/three": "^0.185.0",
    "three": "^0.185.0"
  }
}
